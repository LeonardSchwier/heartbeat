# Runs heartbeat.service twice a day, at 08:00 and 20:00 local time.
# Adjust OnCalendar to taste; see `man systemd.time`.

[Unit]
Description=Run heartbeat twice daily

[Timer]
OnCalendar=*-*-* 08,20:00:00
Persistent=true

[Install]
WantedBy=timers.target
